import type { NextComponentType } from 'next';
import type { AppContext } from 'next/app';
import { MODE } from './config';
import { useDarkMode } from './darkModeContext';
import type { Config } from './config';
interface AppProps {
    autoMode?: boolean;
    darkMode?: boolean;
    initialProps: any;
}
declare const _default: (App: NextComponentType | any, config?: Partial<Config> | undefined) => {
    ({ autoMode, darkMode, initialProps, ...props }: AppProps): JSX.Element;
    getInitialProps(appContext: AppContext): Promise<{
        autoMode: boolean;
        darkMode: boolean;
        initialProps: any;
    } | {
        initialProps: any;
        autoMode?: undefined;
        darkMode?: undefined;
    }>;
    displayName: string;
};
export default _default;
export { MODE, useDarkMode };
